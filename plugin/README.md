# AER Plugin-ready metadata

This directory is the smallest local developer surface for connecting a ChatGPT/App client to AER MCP. Start `aer mcp --http` to expose the loopback endpoint in `manifest.json`. Secure MCP Tunnel is optional; `aer doctor` reports local client readiness while account entitlement remains external and `unverified`.
